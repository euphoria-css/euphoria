import React from 'react'

function Code({ children }) {
  return (
    <code className="code purple-dark bg-purple-lightest px-xs py-xxs rad-xs">
      {children}
    </code>
  )
}

export default Code
